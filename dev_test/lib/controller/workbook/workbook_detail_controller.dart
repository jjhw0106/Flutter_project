import 'package:flutter/cupertino.dart';

class WorkBookDetailController extends StatelessWidget {
  const WorkBookDetailController({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}