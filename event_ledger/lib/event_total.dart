import 'package:flutter/material.dart';

class totalMain extends StatelessWidget {
  const totalMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("집계화면");
  }
}
