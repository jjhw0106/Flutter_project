import 'package:flutter/material.dart';

class sadMain extends StatelessWidget {
  const sadMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
